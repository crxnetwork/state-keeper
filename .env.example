# Copy to .env (gitignored) — secrets arrive ONLY via the environment.
# Overrides the chains.json preset.
RPC_URL=https://forno.celo-sepolia.celo-testnet.org
# Signs the applyState transaction (advance only); needs lane gas.
PRIVATE_KEY=
# Succinct Prover Network requester key (advance only) — the network is the only
# prover, no local or mock path; proving spends a fee. https://docs.succinct.xyz/
NETWORK_PRIVATE_KEY=
