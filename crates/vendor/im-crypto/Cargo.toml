[package]
name = "im-crypto"
version = "0.0.0"
edition = "2021"

[dependencies]
im-types = { path = "../im-types" }
tiny-keccak = { version = "2.0", features = ["keccak"] }

[dev-dependencies]
proptest = "1"

[lints]
workspace = true
