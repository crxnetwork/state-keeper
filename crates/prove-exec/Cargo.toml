# prove-exec — the SP1 surface. EXACT-pin the sp1 stack to 6.3.0 — the toolchain the
# on-chain vkey 0x005283d6… was derived under.
[package]
name = "prove-exec"
version = "0.1.0"
edition = "2021"

[dependencies]
sp1-sdk = { version = "=6.3.0", features = ["blocking", "network"] }
sp1-prover = "=6.3.0"
anyhow = "1"
